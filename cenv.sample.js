const laravel = '/mnt/hgfs/empire/var/www/macentrale/leg2/'

const config = {
    laravel : {
        public : `${laravel}public/`,
        root : laravel,
        host : 'www.leg2.wr'
    }
}

module.exports = config;