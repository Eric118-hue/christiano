const laravel = '/home/admin/www/macentrale/gha/'

const config = {
    laravel : {
        public : `${laravel}public/`,
        root : laravel,
        host : 'localhost'
    }
}

module.exports = config;
